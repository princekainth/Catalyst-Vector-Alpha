# Complete requirements for Catalyst Vector Alpha
# Note: versions are taken from the current environment when available; optional tools are left unpinned if not installed.

# Core web/runtime
Flask==3.1.2
Flask-CORS==6.0.1
waitress==3.0.2

# Config & utils
python-dotenv==1.0.1
PyYAML==6.0.1
requests==2.31.0
urllib3==2.0.7
jsonschema==4.24.0
psutil==7.0.0
python-dateutil==2.8.2
pytz==2024.1
prometheus-client==0.22.0

# Data / math
numpy==2.3.5
pandas  # optional (not installed in env)
matplotlib  # optional (not installed in env)

# AI/LLM/embeddings
chromadb==1.3.5
ollama==0.6.1
sentence-transformers  # optional (not installed in env)
transformers  # optional (not installed in env)

# NLP / parsing / search
beautifulsoup4==4.13.4
ddgs  # optional duckduckgo-search client (not installed in env)

# Reporting / docs
fpdf  # optional (not installed in env)
reportlab  # optional (not installed in env)

# Google APIs
google-auth-oauthlib==1.2.3
google-auth-httplib2==0.2.1
google-api-python-client==2.187.0

# Kubernetes & cloud
kubernetes==34.1.0

# Frontend / app
streamlit  # optional (not installed in env)
playwright  # optional (not installed in env)

# Database
psycopg2-binary==2.9.11
